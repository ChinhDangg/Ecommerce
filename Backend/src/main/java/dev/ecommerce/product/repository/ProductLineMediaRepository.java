package dev.ecommerce.product.repository;

import dev.ecommerce.product.entity.ProductLineMedia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductLineMediaRepository extends JpaRepository<ProductLineMedia, Long> {
}
